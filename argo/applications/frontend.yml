apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend
  namespace: argocd
spec:
  project: default
  destination:
    name: in-cluster
    namespace: default
  source:
    repoURL: https://github.com/devoteamgcloud/dgc-dk-gitops-frontend
    targetRevision: HEAD
    path: ./argo/frontend
  syncPolicy: 
    automated:
      prune: true